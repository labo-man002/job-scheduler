[tool.ruff]
line-length = 145  # matches actual longest existing line (142) plus headroom, not an arbitrary default

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "RUF"]
ignore = [
    "N999",  # filenames here are deliberately camelCase for models/enums, not snake_case -- see CLAUDE.md
]
